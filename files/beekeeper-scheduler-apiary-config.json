{
  "spring.datasource": {
    "url": "jdbc:mysql://${db_endpoint}/${db_name}?useSSL=false",
    "username": "${db_username}"
  },
  "properties": {
    "apiary.queue-url": "${queue}",
    "metastore-uri": "${metastore_uri}"
  }${graphite_config}
}
