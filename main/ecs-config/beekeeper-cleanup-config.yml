spring.datasource:
  url: jdbc:mysql://${endpoint}/beekeeper?useSSL=false
  username: ${username}
properties:
  scheduler-delay-ms: ${scheduler-delay-ms}
  dry-run-enabled: ${dry-run-enabled}
graphite:
  enabled: ${graphite-enabled}
  host: ${graphite-host}
  prefix: ${graphite-prefix}
  port: ${graphite-port}
